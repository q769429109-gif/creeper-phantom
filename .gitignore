# ============================================================
#  Phantom Blast（幻翼爆破）—— Git 排除清单
# ============================================================
#  说明：本清单只影响 git，「被忽略的文件依旧留在本地磁盘上」。
#        想恢复某个目录的提交，把对应行前面加 # 注释掉即可。
# ============================================================

# ---------- Gradle 构建产物（完全可再生产，71 MB） ----------
build/
out/
.gradle/

# 开发运行目录（runServer / runClient 生成的临时世界、EULA、日志）
run/
run-*/

# ---------- Python 缓存（vanilla-reference/tools/ 里的脚本） ----------
__pycache__/
*.py[cod]

# ---------- 日志与临时文件 ----------
*.log
*.bak
*.orig
*.tmp
*~
*.part
*.lck

# ---------- IDE ----------
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.classpath
.project
*.launch

# ---------- 操作系统 ----------
.DS_Store
Thumbs.db
desktop.ini

# ---------- 助手工作笔记（不对外发布） ----------
.workbuddy/

# ---------- 调试期残留目录 ----------
.ref*/

# ============================================================
#  ⚠️ 版权敏感：原版素材与反编译源码
# ============================================================
#  这两个目录是「从 Minecraft 客户端 jar 里抽出来的东西」，属于
#  Mojang / Microsoft 的版权资产，**不适合放进公开仓库**：
#
#    vanilla-reference/model-src/   Minecraft 的反编译 Java 源码
#    vanilla-reference/textures/    Minecraft 的原版实体贴图
#
#  保留在本地随时查阅是没问题的；一旦 push 到公开 GitHub 仓库，
#  可能收到 DMCA 下架通知。默认已排除。
#
#  —— 如果你的仓库是私有的、且只供自己用，可以注释掉下面两行。
# ============================================================
vanilla-reference/model-src/
vanilla-reference/textures/

# ---------- 助手自检渲染图（非工程必需，可自行取舍） ----------
# check_*.png / debug_*.png 是我做模型换算校验时生成的对比图与结构图。
# 想留作开发记录就注释掉下面两行；只在本地看的话保持排除即可。
vanilla-reference/bbmodel/check_*.png
vanilla-reference/bbmodel/debug_*.png
